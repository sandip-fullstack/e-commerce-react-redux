import React from "react";
import Categories from "./Categories";

const SideBar = ()=>{
    return(
        <div>
            <Categories/>
        </div>
    );
};

export default SideBar;